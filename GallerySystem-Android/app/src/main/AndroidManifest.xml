<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.mcgill.ecse321.gallerysystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GallerySystemAndroid">
        <activity android:name=".CustomerDashboardActivity"></activity>
        <activity android:name=".CustomerLoginActivity" />
        <activity android:name=".CustomerRegisterActivity" />
        <activity android:name=".CustomerActivity" />
        <activity android:name=".ArtistAddArtpieceActivity" />
        <activity android:name=".ArtistManageArtpieceActivity" />
        <activity android:name=".ArtistManageProfileActivity" />
        <activity android:name=".ArtistDashboardActivity" />
        <activity android:name=".ArtistLoginActivity" />
        <activity android:name=".ArtistRegisterActivity" />
        <activity android:name=".ArtistActivity" />
        <activity android:name=".ShoppingCartActivity" />
        <activity android:name=".ViewArtpiecesActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GallerySystemAndroid.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>